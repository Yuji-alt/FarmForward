<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Fill color (background inside the border) -->
    <solid android:color="@color/white"/>

    <!-- Border line -->
    <stroke
        android:width="2dp"
        android:color="@color/iron_gray"/>

    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
</shape>
